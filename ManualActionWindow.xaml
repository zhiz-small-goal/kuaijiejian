<Window x:Class="Kuaijiejian.ManualActionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="手动添加 Photoshop 动作" 
        Height="480" 
        Width="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Border Background="White" 
            CornerRadius="12" 
            BorderBrush="#E5E7EB" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.2" Color="#000000"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" 
                    Background="#F9FAFB" 
                    CornerRadius="12,12,0,0" 
                    Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📝" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="手动添加 Photoshop 动作" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="#1F2937"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Content="✕" 
                            Width="28" 
                            Height="28"
                            HorizontalAlignment="Right"
                            Click="CancelButton_Click"
                            Cursor="Hand"
                            ToolTip="关闭">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="#6B7280"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          Padding="25,20">
                <StackPanel>
                    <!-- 帮助提示 -->
                    <Border Background="#EFF6FF" 
                            BorderBrush="#BFDBFE" 
                            BorderThickness="1" 
                            CornerRadius="8"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="💡" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="如何找到动作名称？" 
                                           FontSize="13" 
                                           FontWeight="SemiBold"
                                           Foreground="#1E40AF"/>
                            </StackPanel>
                            <TextBlock TextWrapping="Wrap" 
                                       FontSize="12" 
                                       Foreground="#1E40AF"
                                       LineHeight="18">
                                1. 打开 Photoshop<LineBreak/>
                                2. 按 Alt+F9 打开 Actions 面板<LineBreak/>
                                3. 找到动作集（文件夹图标）和动作名称<LineBreak/>
                                4. 复制粘贴到下方输入框中
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- 动作集名称输入 -->
                    <TextBlock Text="动作集名称 *" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#374151"
                               Margin="0,0,0,8"/>
                    <Border BorderBrush="#D1D5DB" 
                            BorderThickness="1" 
                            CornerRadius="6"
                            Margin="0,0,0,5">
                        <Grid>
                            <TextBox x:Name="ActionSetTextBox" 
                                     Padding="12,10"
                                     FontSize="13"
                                     BorderThickness="0"
                                     TextChanged="ActionSetTextBox_TextChanged"/>
                            <TextBlock x:Name="ActionSetPlaceholder"
                                       Text="例如：Default Actions"
                                       FontSize="13"
                                       Foreground="#9CA3AF"
                                       Margin="13,10"
                                       IsHitTestVisible="False"
                                       Visibility="Visible"/>
                        </Grid>
                    </Border>
                    <TextBlock x:Name="ActionSetError"
                               Text="⚠ 请输入动作集名称"
                               FontSize="11"
                               Foreground="#EF4444"
                               Margin="5,3,0,0"
                               Visibility="Collapsed"/>
                    
                    <!-- 动作名称输入 -->
                    <TextBlock Text="动作名称 *" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#374151"
                               Margin="0,20,0,8"/>
                    <Border BorderBrush="#D1D5DB" 
                            BorderThickness="1" 
                            CornerRadius="6"
                            Margin="0,0,0,5">
                        <Grid>
                            <TextBox x:Name="ActionNameTextBox" 
                                     Padding="12,10"
                                     FontSize="13"
                                     BorderThickness="0"
                                     TextChanged="ActionNameTextBox_TextChanged"/>
                            <TextBlock x:Name="ActionNamePlaceholder"
                                       Text="例如：Wood Frame - 50 pixel"
                                       FontSize="13"
                                       Foreground="#9CA3AF"
                                       Margin="13,10"
                                       IsHitTestVisible="False"
                                       Visibility="Visible"/>
                        </Grid>
                    </Border>
                    <TextBlock x:Name="ActionNameError"
                               Text="⚠ 请输入动作名称"
                               FontSize="11"
                               Foreground="#EF4444"
                               Margin="5,3,0,0"
                               Visibility="Collapsed"/>

                    <!-- 预览信息 -->
                    <Border Background="#F9FAFB" 
                            BorderBrush="#E5E7EB"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="12"
                            Margin="0,20,0,0"
                            Visibility="{Binding ElementName=PreviewPanel, Path=Visibility}">
                        <StackPanel x:Name="PreviewPanel" Visibility="Collapsed">
                            <TextBlock Text="预览" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"
                                       Foreground="#6B7280"
                                       Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="按钮显示：" 
                                           FontSize="12" 
                                           Foreground="#6B7280"
                                           Margin="0,0,5,0"/>
                                <TextBlock x:Name="PreviewText" 
                                           FontSize="12" 
                                           FontWeight="SemiBold"
                                           Foreground="#1F2937"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- 底部按钮 -->
            <Border Grid.Row="2" 
                    Background="#F9FAFB"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12"
                    Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" 
                            Width="90" 
                            Height="36"
                            Margin="0,0,10,0"
                            Click="CancelButton_Click"
                            IsCancel="True"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F9FAFB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="AddButton"
                            Content="添加动作" 
                            Width="100" 
                            Height="36"
                            Click="AddButton_Click"
                            IsDefault="True"
                            IsEnabled="False"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#8B5CF6"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7C3AED"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#D1D5DB"/>
                                        <Setter Property="Foreground" Value="#9CA3AF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

